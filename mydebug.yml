---
- hosts: 172.31.21.105
  become: yes
  tasks:
  - name: i want to execute linux command
    command: hostname -f
    register: myResult

- name: i want to display myfrist task myResult
  debug: 
     msg: "{{myResult}}"    